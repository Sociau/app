import { StatusBar } from 'react-native';

export function preventAutoHide() {
    // Placeholder para controle manual de splash (se você quiser uma imagem splash manual)
}

export function hide() {
    StatusBar.setHidden(false);
}
